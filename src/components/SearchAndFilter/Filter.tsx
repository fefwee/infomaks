import { FC } from "react";
import sort from '../../../public/Sort.png'

export const Filter: FC = () => {
    return (
        <div>
            <span><img src={sort} alt="sort" />Сначала в наличии</span>

        </div>
    );
};
